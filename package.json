{
  "name": "@workspace/root",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck",
    "mobile:install": "cd artifacts/mobile && pnpm install",
    "mobile:start": "cd artifacts/mobile && pnpm start",
    "mobile:android": "cd artifacts/mobile && pnpm android",
    "mobile:build-debug": "cd artifacts/mobile/android && ./gradlew assembleDebug",
    "mobile:build-release": "cd artifacts/mobile/android && ./gradlew assembleRelease"
  },
  "dependencies": {
    "@replit/connectors-sdk": "^0.4.1"
  },
  "devDependencies": {
    "prettier": "^3.8.4",
    "typescript": "~5.9.3"
  }
}
